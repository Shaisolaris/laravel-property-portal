<div class="progress animated-progress">
    <div
        class="progress-bar bg-{{$variable}}"
        role="progressbar"
        style="width: {{$percentage}}%"
        aria-valuenow="{{$percentage}}"
        aria-valuemin="{{$min}}"
        aria-valuemax="{{$max}}"
    ></div>
</div>